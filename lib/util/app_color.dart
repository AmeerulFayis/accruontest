import 'dart:ui';

class AppColor{
static const blue = Color(0xff000052);
static const lightBlue = Color(0xff0a0ad2);
static const black = Color(0xff000000);
static const white = Color(0xffffffff);

}